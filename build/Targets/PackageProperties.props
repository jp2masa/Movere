<Project>

    <PropertyGroup>
        <PackageVersion Condition="'$(PackageVersionSuffix)' != ''">$(PackageVersion)$(PackageVersionSuffix)</PackageVersion>
    </PropertyGroup>

    <PropertyGroup>
        <BaseDescription>Movere is an implementation of managed dialogs for Avalonia.</BaseDescription>
    </PropertyGroup>
    
    <PropertyGroup>
        <Authors>jp2masa</Authors>
        <Copyright>Copyright © $([System.DateTime]::Now.Year) jp2masa</Copyright>
        <PackageIcon>Icon.png</PackageIcon>
        <PackageProjectUrl>https://github.com/jp2masa/Movere</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageRequireLicenseAcceptance>True</PackageRequireLicenseAcceptance>
        <PackageTags>movere avalonia dialogs dotnet core csharp vb visual basic fsharp</PackageTags>
        <PublishRepositoryUrl>true</PublishRepositoryUrl>
    </PropertyGroup>

    <ItemGroup>
        <None Include="$(RepoRoot)Icon.png" Pack="true" PackagePath="\" Visible="false" />
        <None Include="$(RepoRoot)README.md" Pack="true" PackagePath="\" Visible="false" />
    </ItemGroup>

</Project>
